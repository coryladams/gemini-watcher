# Gemini Vault Watcher

Automated Obsidian vault processing via Gemini CLI and WSL.

## Setup
1. Install watchdog: `pip install watchdog`
2. Configure `gemini_config.json`
3. Import `vault-watcher-task.xml` into Windows Task Scheduler.